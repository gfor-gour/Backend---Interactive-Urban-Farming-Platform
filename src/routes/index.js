/**
 * Routes Module
 * Defines all API endpoints and route structure
 * Routes delegate to controllers, never contain business logic
 * 
 * Example structure:
 * - GET /api/v1/users -> usersController.getUsers()
 * - POST /api/v1/users -> usersController.createUser()
 * - GET /api/v1/users/:id -> usersController.getUserById()
 * - PUT /api/v1/users/:id -> usersController.updateUser()
 * - DELETE /api/v1/users/:id -> usersController.deleteUser()
 */

export default {};
